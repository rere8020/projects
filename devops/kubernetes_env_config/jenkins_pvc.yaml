apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: azure-pvc
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: jenkins-azurefile
  resources:
    requests:
      storage: 5Gi